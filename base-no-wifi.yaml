---
substitutions:
  name: REPLACEME

esphome:
  name: $name

logger:
  baud_rate: 0

ota:
  platform: esphome

safe_mode:

api:

web_server:
  local: true
  version: 3

button:
  - platform: restart
    id: restart_button
    icon: mdi:restart
    name: Restart

  - platform: safe_mode
    id: safe_mode_button
    icon: mdi:restart-alert
    name: Safe Mode

sensor:
  - platform: uptime
    name: Uptime
