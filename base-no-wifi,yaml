---
substitutions:
  name: REPLACEME

esphome:
  name: $name

logger:
  baud_rate: 0
  logs:
    component: ERROR

ota:
  platform: esphome

safe_mode:

api:

web_server:
  local: true
  version: 2

button:
  - platform: restart
    id: restart_button
    icon: mdi:restart
    name: $name Restart

  - platform: safe_mode
    id: safe_mode_button
    icon: mdi:restart-alert
    name: $name Safe Mode

sensor:
  - platform: uptime
    name: $name Uptime
